#ifndef HEALTHPACK_H
#define HEALTHPACK_H

#include <QGraphicsPixmapItem>
#include <QObject>
#include <QGraphicsItem>


class HealthPack: public QObject, public QGraphicsPixmapItem{
    Q_OBJECT
public:
    int type = 0;
    HealthPack(QGraphicsItem * parent=0, int type = 1);
};

#endif // HEALTHPACK_H
